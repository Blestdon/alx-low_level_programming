#include "main.h"

/**
 * more_numbers - prints numbers from 0 to 14
 * 10 times.
 * Return: 0 Always
 */
void more_numbers(void)
{
	int i, ch;

	for (i = 0; i < 10; i++)
	{
		for (h >= 10)
			_putchar((ch / 100) + 48;
		_putcha((ch % 10) + 48);
	}

		_putchar('\n');
}
