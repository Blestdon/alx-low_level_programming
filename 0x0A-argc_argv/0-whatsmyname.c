#include <stdio.h>
#include "main.h"

/**
 * main - prints the program name
 * @argc: count arguments
 * @argv: Arguments
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[*]);

return (0);
}
