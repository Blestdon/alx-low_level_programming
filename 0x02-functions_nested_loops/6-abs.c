#include "main.h"

/**
 *_abs - Computes the absolute value of an integer
 *@z: Integer for computation
 *Return: The absolute value
 */
int _abs(int y)
{
	if (y >= 0)
		return (y);
	else
		return (-y);
}
