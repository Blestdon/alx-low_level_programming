#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *@: integer for computation
 *Return: The absolute value
 */
int _abs(int z)
	if (z >= 0)
		return (z);
	else
		return (-z);

