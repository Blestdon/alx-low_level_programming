#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @s: one of the integers
 * @t: the second integer
 * Return: The sum of both
 */
int add(int s, int t)
{
	return (s + t);
}
